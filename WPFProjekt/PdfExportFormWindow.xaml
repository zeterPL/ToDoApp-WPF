<Window x:Class="WPFProjekt.PdfExportFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFProjekt"
        mc:Ignorable="d"
        Title="PDF Export" Height="450" Width="550">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="90"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0">Select categories</Label>
            <StackPanel Orientation="Horizontal" Grid.Column="1" Margin="15 0">
                <CheckBox Checked="AllCB_Checked" Unchecked="AllCB_Unchecked" x:Name="AllCB" VerticalAlignment="Center"/>
                <Label>All</Label>
            </StackPanel>

            <ComboBox x:Name="CategoriesComboBox" Grid.Column="2" Margin="10 0">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0 10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0">Add title</Label>
                <CheckBox x:Name="titleCB" Grid.Column="1" VerticalAlignment="Center"/>
                
                <TextBox x:Name="titleTextBox" Grid.Column="2" Margin="10 0" IsEnabled="{Binding ElementName=titleCB, Path=IsChecked}"></TextBox>
                <TextBlock Grid.Column="2" IsHitTestVisible="False" Text="Title" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="15,0,0,0" Foreground="DarkGray">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=titleTextBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

           
            
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Label  VerticalAlignment="Center">Add current date</Label>
                <CheckBox Checked="DataCB_Checked" Unchecked="DataCB_Unchecked" VerticalAlignment="Center" x:Name="DataCB"/>
            </StackPanel>
            
            <GroupBox Grid.Column="1" Margin="10 0">
                <GroupBox.Header>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Checked="DiffDateCB_Checked" Unchecked="DiffDateCB_Unchecked" x:Name="DiffDateCB"/>
                        <TextBlock>Inna data</TextBlock>
                    </StackPanel>
                </GroupBox.Header>

                <DatePicker x:Name="DatePicker1" IsEnabled="{Binding ElementName=DiffDateCB, Path=IsChecked}"/>
            </GroupBox>
        </Grid>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            
            <GroupBox Grid.Column="0" Margin="10 8" Header="Select sort mode:">
                <StackPanel>
                    <RadioButton x:Name="NoSortRB" Margin="5" IsChecked="True">None</RadioButton>
                    <RadioButton x:Name="PrioritySortRB" Margin="5">By Priority</RadioButton>
                    <RadioButton x:Name="TitleSortRB" Margin="5">By Title</RadioButton>
                </StackPanel>
            </GroupBox>


            <GroupBox Grid.Column="1" Margin="10 8" Header="Print categories titles?">
                <StackPanel>
                    <RadioButton x:Name="catTitlePrintRB" IsChecked="True">Yes</RadioButton>
                    <RadioButton x:Name="NocatTitlePrintRB">No</RadioButton>
                </StackPanel>
            </GroupBox>

            <GroupBox Grid.Column="2" Margin="10 8">
                <GroupBox.Header>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox x:Name="FieldsCB"/>
                        <TextBlock>Select note fields to print</TextBlock>
                    </StackPanel>
                </GroupBox.Header>
                <StackPanel IsEnabled="{Binding ElementName=FieldsCB, Path=IsChecked}">
                    <CheckBox x:Name="TitleCB">Title</CheckBox>
                    <CheckBox x:Name="ContentCB">Content</CheckBox>
                    <CheckBox x:Name="PriorityCB">Priority</CheckBox>
                    <CheckBox x:Name="cdateCB">Create Date</CheckBox>
                    <CheckBox x:Name="edateCB">Edit Date</CheckBox>
                </StackPanel>
            </GroupBox>



        </Grid>

        <Button VerticalAlignment="Bottom" Padding="4" HorizontalAlignment="Right" Click="GeneratePdf" Grid.Row="4" Margin="8" Content="OK" Width="50"/>

    </Grid>
</Window>
